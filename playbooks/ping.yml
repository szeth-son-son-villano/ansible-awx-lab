---
- name: Test AWX connectivity
  hosts: all
  gather_facts: true

  tasks:

    - name: Ping host
      ansible.builtin.ping:

    - name: Show hostname
      ansible.builtin.debug:
        var: ansible_hostname
